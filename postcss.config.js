module.exports = {
  plugins: [
    require('autoprefixer'),
    // require('css-mqpacker'), it collects media queries along whole project to one place
    //require('cssnano') it compress CSS
  ]
};
